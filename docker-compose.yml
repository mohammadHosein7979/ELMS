version: "3.9"
services:
  app:
    image: ${DC_IMAGE_NAME}:${DC_IMAGE_TAG}
    container_name: ${DC_CONTAINER_NAME}
    networks:
      - network
    restart: always

networks:
  network:
    external:
      name: ${DC_NETWORK}